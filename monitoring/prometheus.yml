global:
  scrape_interval: 30s
  evaluation_interval: 30s

  external_labels:
    monitor: 'monitor'

scrape_configs:
  - job_name: 'minitwit'
    scrape_interval: 30s
    static_configs:
      - targets: ['razor-pages:8080']