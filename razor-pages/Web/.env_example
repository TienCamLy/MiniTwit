api_token=example_token

# EF / Npgsql (same key as CI; pick host per run mode):
# dotnet run on host:     Server=localhost;...
# docker compose up:       Server=host.docker.internal;...
CONNECTIONSTRINGS__DEFAULTCONNECTION=Server=localhost;Database=defaultdb;User Id=ci_tester;Password=ci_tester;